{
  "name": "kdn-sport-complex",
  "version": "1.0.0",
  "description": "KDN Sport Complex website with Gym, Badminton Court, Supplement Shop, Physiotherapy booking, Admin dashboard, and Secret Gatekeeper site shutdown feature.",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
